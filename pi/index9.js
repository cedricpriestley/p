import sqlite3 from 'sqlite3'
console.log("hello");
// import { open } from 'sqlite'
// // this is a top-level await 
// (async () => {
//   // open the database
//   const db = await open({
//     filename: '/data/data/com.termux/files/home/projs/p/pi/db/users.db',
//     driver: sqlite3.Database
//   })
// })()

// await db.exec('CREATE TABLE tbl2 (col TEXT)')
// await db.exec('INSERT INTO tbl2 VALUES ("test")')

// await db.close()
